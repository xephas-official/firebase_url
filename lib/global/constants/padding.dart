import '../../app_exporter.dart';

//// * App Paddings following the 8px grid system

/// padding 0
const padding0 = EdgeInsets.zero;

/// padding 2
const padding2 = EdgeInsets.all(spacing2);

/// padding 4
const padding4 = EdgeInsets.all(spacing4);

/// padding 8
const padding8 = EdgeInsets.all(spacing8);

/// padding 16
const padding16 = EdgeInsets.all(spacing16);

/// padding 24
const padding24 = EdgeInsets.all(spacing24);

/// padding 32
const padding32 = EdgeInsets.all(spacing32);

/// padding 40
const padding40 = EdgeInsets.all(spacing40);

///* Vertical Paddings
const verticalPadding4 = EdgeInsets.symmetric(vertical: spacing4);

/// padding 8
const verticalPadding8 = EdgeInsets.symmetric(vertical: spacing8);

/// vertical padding 16
const verticalPadding16 = EdgeInsets.symmetric(vertical: spacing16);

///  32 vertical padding
const verticalPadding32 = EdgeInsets.symmetric(vertical: spacing32);

/// * Horizontal Paddings
/// 2
const horizontalPadding2 = EdgeInsets.symmetric(horizontal: spacing2);

/// 4
const horizontalPadding4 = EdgeInsets.symmetric(horizontal: spacing4);

///
const horizontalPadding8 = EdgeInsets.symmetric(horizontal: spacing8);

/// 16
const horizontalPadding16 = EdgeInsets.symmetric(horizontal: spacing16);

/// 32
const horizontalPadding32 = EdgeInsets.symmetric(horizontal: spacing32);
